.modal-dialog.modal-lg
  .modal-content
    .modal-header
      button(type="button" class="close" data-dismiss="modal") &times;
      button.btn.btn-default.btn-ms(tyle = 'button' href = '#'
                                    data-dismiss = 'modal' data-toggle = 'modal'
                                    style = 'position: absolute; left: 2%'
                                    data-target = '#editStaffModal')
h4.modal-title(style="text-align-last:center") Staff Info
    .modal-body
      .container-fluid
        //enter content
        form.form-horizontal(role='form', action="/addstaff" method="post")
            include staffForm.pug
            .form-group
                    button.btn.btn-default(type='submit') Submit
